apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: gearflow-jwt
  namespace: gearflow
plugin: jwt
config:
  key_claim_name: iss
  claims_to_verify:
    - exp
  # Apenas rotas com esta anotação são protegidas; /auth/* fica aberto
