apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gearflow-ingress-public
  namespace: gearflow
  annotations:
    # Path mais específico que /api → Kong prioriza esta rota. Login/registro/refresh
    # são anônimos por natureza (é como o staff OBTÉM o JWT); sem o plugin gearflow-jwt aqui.
    konghq.com/plugins: gearflow-rate-limit
spec:
  ingressClassName: kong
  rules:
    - http:
        paths:
          - path: /api/identity/auth
            pathType: Prefix
            backend:
              service:
                name: gearflow-api-service
                port:
                  number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gearflow-ingress
  namespace: gearflow
  annotations:
    # Aplica JWT em todas as rotas /api/* e rate-limit em /auth/*
    konghq.com/plugins: gearflow-jwt,gearflow-rate-limit
spec:
  ingressClassName: kong
  rules:
    - http:
        paths:
          # /auth/* → Lambdas (gearflow-lambda via ExternalName service)
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: lambda-proxy-svc
                port:
                  number: 443
          # /api/* → aplicação .NET (gearflow-app)
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: gearflow-api-service
                port:
                  number: 8080
