# ── Imagem da API ─────────────────────────────────────────────────────────
# A imagem é publicada pelo pipeline do gearflow-app (Repo 4) no Docker Hub.
# O Kubernetes faz pull automático — não é necessário injetar manualmente.
# Referenciada via var.api_image no gearflow-app/terraform ou k8s/api/deployment.yaml.
